import { defineConfig } from 'dumi';

export default defineConfig({
  title: 'Bubblyface.github.io',
  favicon: '/images/logo.png',
  logo: '/images/logo.png',
  outputPath: 'docs-dist',
  mode: 'site',
  // more config: https://d.umijs.org/config
});
